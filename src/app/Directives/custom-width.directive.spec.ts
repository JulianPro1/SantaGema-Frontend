import { CustomWidthDirective } from './custom-width.directive';

describe('CustomWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomWidthDirective();
    expect(directive).toBeTruthy();
  });
});
